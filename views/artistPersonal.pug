doctype html
html(lang="en")
    head
        title Artist Page
        link( href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous")
        style.
            input[type=text]{
                width:30%;
                padding: 1px 22px;
                margin: 10px 5px;
                box-sizing: border-box;
                text-align:center;
            }
            body{
                text-align:center;
                background-color: lightgrey;
            }
    body
        script(src='addReview.js')
        include ./header
        br
        -var nameOfArtist
        -for(let i=0; i < length; i++)
            -if(artworks[i].id===id)
                h1 #{artworks[i].artist}'s Page
                -nameOfArtist = artworks[i].artist
        a(href = "/artists/" + id + "/artist/") Artworks!
        h2 Workshop
        h3 Click to join this artist's workshop today!
        button(onclick='alert("You are officially enrolled in the workshop!")') Enroll
        br 
        br
        br
        button(onclick='alert("You are now following")') Follow!
        h2 Add a review to this artist's page!
        form(action="/artists/:id", method="POST")
            input(type='text', name='review', value='', id='review')
            br
            button(onclick="javascript:submit()") Post!
        br
        a(href = "/workshops") Workshops!
        br
        h3 Reviews 
        ul  
            -for(let i=0; i< reviews.length; i++)
                li #{reviews[i]}
        